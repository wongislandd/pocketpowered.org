{
  "name": "pocketpowered-org",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "npm run types && npm run typecheck",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
